apiVersion: v1
kind: Pod

metadata:
  name: gpu-test

spec:
  runtimeClassName: nvidia
  restartPolicy: Never

  containers:
    - name: gpu-test
      image: nvidia/cuda:13.0.0-base-ubuntu24.04

      command:
        - nvidia-smi

      resources:
        limits:
          nvidia.com/gpu: 1